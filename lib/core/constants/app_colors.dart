import 'package:flutter/material.dart';

class AppColors {
  // --- BRAND COLORS ---
  static const Color brandGreen = Color(0xFF2E7D32); // Dark Green
  static const Color primary = Color(0xFF2E7D32); 
  
  // --- UI COLORS ---
  static const Color background = Color(0xFFFAFAFA); // Almost White
  static const Color surface = Colors.white;         
  
  // Matches the "Hello Ramesh" card exactly
  static const Color creamBackground = Color(0xFFFFF8E1); 
  
  // Matches the "Pending" text
  static const Color pendingStatus = Color(0xFFFF8A65);   
  
  // Matches the Alert text
  static const Color alertText = Color(0xFFC62828);      
  
  // --- TEXT ---
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnButton = Colors.white; 
  
  // --- BORDERS ---
  static const Color border = Color(0xFFEEEEEE);
}